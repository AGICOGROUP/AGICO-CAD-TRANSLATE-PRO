# CAD 双语区域排版重构检查点

## 状态

代码、Skill 指令、原生验证及实图视觉验收已完成。验证过的 DLL 已覆盖 assets/plugin，bin/text-validation 的 Core 与其哈希一致。旧包备份在 tmp/package-before-outside-20260916。分支 codex/bilingual-region-layout；未提交、未推送。尚未证明整体提速。

## 覆盖后的规则与实现

- 保留 CAD 原生读写、源实体保护及单语/双语独立流程，仅改造双语区域识别和排版。
- 先投影匿名/嵌套块的表格线，再建立完整表格索引，避免表头和正文分流。排除连接整页的外围伪单元格；标题栏按字段位置识别。
- 宽松单元格优先同行追加；拥挤表格完整复制。短图例按原行对齐；成块说明保持完整阅读顺序，不逐条散排。
- 用户已批准：完整译文表和大段说明可放在同侧最近的框外空白处。先尝试可读框内候选，再尝试图框四侧；不得进入相邻图纸内部。零散短标签仍就近配对。
- 不移动原图、不修改打印设置；DWG 交付明示旧打印窗口/视口可能遗漏附表。打印版/PDF 必须验证实际包含。
- 复制纯网格时新建 BlockReference，复用定义、外观、变换，不复制关联反应器。保留精确几何验证；仅一项待译也整体处理。
- 单元格根据实际字体测量并按需换行，最低字高为原文 60%；纠正实际换行宽度略超配置宽度，不放松边界。
- 修复 Rect2 JSON 边界归零、诊断器双语风险类型错误，补记局部纠正用时；覆盖精简 Skill 及参考流程。

## 验证证据

- Core：205 项通过，退出码 0。
- Python：123 项及 39 子测试通过（python -m pytest tests --ignore=tests/cad -q）。全目录曾有 3 个 tests.cad 包解析错误，未声称集成套件全部通过。
- AutoCAD 2025 / .NET 8 原生构建通过；已有 Autodesk 引用警告仍存在。TextCli 构建 0 错误。
- tmp/region-native-table-final/report.json：宽松单元格同行追加通过。
- tmp/region-clean-grid-final/report.json：嵌套网格、全块内网格、单项待译、混合图形拒收、框外完整复制、真实字体单元格适配通过。
- tmp/grid-probe/compare-r6.json：原网格 31 条实体记录不变；原关联依赖保持 IsUpToDateAssocStatus；副本无持久反应器。
- Skill 格式与 git diff --check 通过。部署 DLL 与实图验证运行目录哈希逐一一致；源 DWG 哈希不变。

## 实图回归

源：D:/测试文件/CAD/实验线工艺260817（可使用场地面积、高度、行车位置布置）.dwg

源 SHA256：90052a65a1e2d972f2bb7d1179519e0e0ed44db4baa6195232f74fd6fd1f85d4

首轮 jobs/experimental-line-region-v2-20260916 全新提取、翻译，未用历史译文：648 源记录、203 个去重翻译项、229 个需翻译实体。同次测试恢复到 -r6，沿用本轮已校验译文。

旧问题：正文横线藏于 *U143 嵌套行块，只复制表头，其余散排。现识别完整 98 单元格、15 行正文及不规则表头。r3 因相邻空间不足剩 41 项；用户批准框外完整附表后继续。

r4 暴露实际字体/留白过紧，修复后 r5 导入成功，但视觉发现原表横线消失，未接受。几何对照证明原实体未变，是直接 Clone 复制关联依赖导致原实例显示状态异常；改为新建干净网格实例后，r6 原表及副本正常。

最终：jobs/experimental-line-region-v2-20260916-r6/results/candidate.dwg

候选 SHA256：0aabaaf3e1e1b027ed6c2302d68ab87c130e8f96709a4ce641afaaaa5404ebc0

保留 648 源记录，新增 183 处、复用 46 处；完整复制 68 个表实体、翻译 41 单元格。未放置、源修改、目标缺失均为 0。对照表格、图例、流程细节，检查三张流程页、设备页及标题栏。英文整表置于原表上方框外。最终审计 deliveryReady=true / ready_with_warnings。

提醒：打印需包含框外附表；部分长单元格两行/较小字高，密集图例仍紧凑。本图不能证明所有大段说明场景都有实图覆盖；说明整体框外候选与相邻图框保护有核心行为测试。

## 用时与限制

首轮工作清单到译文组装约 427 秒（含开发间隙，非纯模型耗时）。r6 原生导入 29.50 秒：拓扑 11.34 秒，边界投影/分类 10.73 秒，附表复制 0.33 秒，普通放置 2.61 秒，验证 3.79 秒。

首次导出到验收 2945.707 秒，约 49.1 分钟，包含开发修复、多个候选及命令间等待，不含首次导出前开发时间。不能把最后一次导入时间当总耗时；未证明整体提速 20%–50% 或保证 20 分钟完成。预期收益是减少表识别错误及无效重试，需后续全新样本同质量对照衡量。
