interface:
  display_name: "CAD Translate Pro"
  short_description: "Chinese-English CAD replacement or additive bilingual translation"
  default_prompt: "Use $translate-cad-files with the user's requested language direction and output mode: replace source text, or preserve it and add nearby translation. Use the selected mode's independent pipeline and verification."
